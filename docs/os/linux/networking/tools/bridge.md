## bridge
- Bridge -> Controller
- NICs -> Ports


Bridge will allow devices connected to one port to contact the devices connected to the second port

```shell
sudo apt install -y openvswitch-switch
```
