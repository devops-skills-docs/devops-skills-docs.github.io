## dig
The dig command queries` Domain Name Systems (DNS)` and finds information for DNS records. The command collects domain 
name information and associated records.
```shell
dig [options] [domain] [record type] [DNS server]
```