## nslookup
The `nslookup` command is similar to the `dig` command. The main difference between the two commands is that nslookup 
features an interactive mode.
```shell
nslookup [domain] [DNS server]
```