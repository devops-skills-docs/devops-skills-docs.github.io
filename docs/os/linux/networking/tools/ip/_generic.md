## ip
The ip command helps view and configure routing, interfaces, network devices, and tunnels.

The command is part of the `iproute2` package and replaces many older networking tools, such as the `route`, `ifconfig`, 
and `netstat` commands.
```shell
ip
```