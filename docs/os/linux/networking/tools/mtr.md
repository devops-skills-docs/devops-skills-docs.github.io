## mtr
The mtr command (my traceroute) is a diagnostics tool that combines elements from the `ping` and `traceroute` commands. 

The command sends real-time insights into network quality, making it an excellent tool for troubleshooting high latency 
and `packet loss`.
```shell
mtr [options] [hostname/IP]
```